<div class="container">
  <div class="row">
    <div class="col-6 offset-3">
      <%= simple_form_for([@restaurant, @review]) do |f| %>
       <%= f.input :content %>
       <%= f.rating :rating %>
       <%= f.submit "submit review" %>
      <% end %>
    </div>
  </div>
</div>
